# 智能管家 App PRD

## 一、产品概述

智能管家 App 是一款**多目标长期生命周期智能管家对话助手**，帮助用户同时管理多个长期目标（如考研备考、孕期管家、证书备考等），通过统一入口与专属会话结合的方式，实现长期记忆沉淀与场景化智能辅助。

## 二、会话体系

产品分为两类会话：

### 2.1 主对话（全局总入口）

- 统一交互大厅，支持闲聊、创建新目标、全局查询所有事项、跨事项复盘。
- 主对话**没有会话隔离限制**，所有提炼完成的有效记忆摘要默认全部可读。

### 2.2 子对话（单一目标专属会话）

- 每一条长期目标对应一个独立子对话（考研备考、孕期管家、证书备考等）。
- 子对话专注处理单一事项，天然具备上下文隔离。
- 仅能读取大模型判定与自身场景相关、且已完成关联绑定的记忆。

## 三、核心业务规则

### 3.1 原始对话日志不直接作为长期记忆

所有主对话、子对话产生的原始聊天流水仅做归档，**不作为推理记忆使用**。

### 3.2 唯一一套定时增量提炼任务（周期 2 小时）

- 任务范围：扫描过去 2 小时内，主对话、所有子对话新增的全部原始对话记录。
- 执行动作：交给 LLM 做增量信息提炼，只提取具备长期价值、需要持久保存的事实信息；过滤闲聊、临时问句、无效对话，不流水式全盘保存。
- 提炼产出一条独立记忆摘要记录，该记忆摘要**初始不绑定任何子对话 ID**。
- 重点：不存在「把摘要统一更新到主对话」。主对话天然有权限读取全部无绑定记忆摘要；子对话想读取，需要后续通过关联判定建立绑定。

### 3.3 记忆关联判定逻辑（异步执行）

提炼生成全新记忆摘要后，触发 LLM 关联推理：

- 输入：本条记忆内容 + 当前用户全部活跃子对话的场景描述 `session_desc`
- 输出：哪些子对话适用这条记忆
- 判定适用：在关联表建立「记忆 ID ↔ 子会话 ID」绑定关系
- 判定不适用：不建立任何关联

### 3.4 记忆读取隔离规则

- **主对话查询记忆**：读取所有无绑定限制的记忆摘要（全部提炼后的记忆默认可见）；如需查看细分事项完整上下文，可按需定向检索。
- **子对话查询记忆**：只能读取关联表中和自身 ID 绑定的记忆摘要；未建立绑定的记忆无法访问，实现场景隔离。

### 3.5 存储模型：两张表实现多对多绑定，不使用数组字段

- 一张表存放提炼后的记忆摘要。
- 一张独立关联表维护记忆与子对话的绑定关系，一条记忆可关联多个子会话。

### 3.6 子会话生命周期兼容

子会话归档关闭后，**不删除记忆摘要、不删除关联关系**。主对话仍可调取全部历史记忆复盘。

## 四、完整场景示例

> 家中突发事务，考研 + 考证计划受影响

1. 用户在考研子对话发送：「家里急事，未来 3 个月无法学习，备考暂停」，原始对话入库归档。
2. 等待 2 小时定时提炼任务启动：
   - 扫描新增对话，过滤无效文本；
   - 提炼有效记忆：【用户家庭突发急事，未来 3 个月无法开展学习类计划】；
   - 生成一条记忆摘要，初始无任何子会话绑定。
3. 自动触发关联推理：
   - LLM 读取考研子对话、考证子对话的场景描述；
   - 判定本条信息对两个事项都生效；
   - 在关联表插入两条绑定：记忆 ID → 考研子会话 ID、记忆 ID → 考证子会话 ID。
4. 访问权限表现：
   - ✅ 主对话：直接看到这条记忆
   - ✅ 考研子对话：可以读到这条记忆
   - ✅ 考证子对话：可以读到这条记忆
   - ❌ 孕期子对话：没有绑定关系，看不到这条记忆
5. 子对话内部根据读到的记忆，自主调整计划、任务清单。
