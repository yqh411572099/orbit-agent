# 智能管家 App 技术方案

## 一、整体架构

采用标准 Agent 三层架构：

```
感知层 → 记忆层 → 行动层
```

技术栈：

- Java 21
- Spring Boot
- Spring AI Alibaba
- DDD（领域驱动设计）思想架构
- 每创建一个场景都是单独的域，支持热部署

## 二、感知层

感知层负责接收交互、路由分发与 LLM 推理入口。

### 2.1 对话网关

接收用户在主对话 / 子对话发送的消息，完成鉴权、参数校验，转发至对应业务流程；原始对话统一归档入库。

### 2.2 会话路由模块

- 用户在主对话指令创建目标：路由至新建子会话流程。
- 用户在子对话反馈信息：执行事项内任务调整推理，更新任务清单。
- 用户查询信息：路由至记忆层，根据会话类型执行不同记忆检索规则。

### 2.3 LLM 推理适配器

统一封装模型调用，强制输出结构化 JSON。对外提供三类推理能力：

1. **目标创建推理**：收集信息、生成初始规划、生成子对话 `session_desc` 场景描述。
2. **任务调整推理**：子会话收到新信息时，更新阶段规划与待办任务。
3. **增量提炼 & 关联推理**：定时任务调用，从原始对话提取有效记忆；再判断记忆适配哪些子会话。

### 2.4 异步任务队列

记忆关联推理异步执行，不阻塞定时提炼主流程；推理失败自动降级：暂时不建立任何关联，等待下一轮 2 小时任务重试。

## 三、记忆层（核心存储与权限控制）

### 3.1 数据表设计

两张核心表实现多对多关联，不使用数组字段。

**表 1：`user_memory`（提炼后的长期记忆摘要主表）**

| 字段 | 说明 |
|------|------|
| memory_id | 记忆唯一 ID |
| user_id | 用户 ID |
| content | 提炼后的结构化记忆文本 |
| create_time | 创建时间 |
| update_time | 更新时间 |

> 本表记录不携带任何子会话 ID，全局中立存储。

**表 2：`memory_session_rel`（记忆 - 子会话关联表，多对多）**

| 字段 | 说明 |
|------|------|
| rel_id | 关联唯一 ID |
| memory_id | 关联记忆 ID |
| sub_session_id | 子会话 ID |
| bind_time | 绑定时间 |

**配套辅助数据表**

- `main_session`：主对话元数据，存储用户全局偏好，不存储记忆摘要。
- `sub_session`：子对话元数据表（`sub_session_id`、`mission_id`、`session_desc` 场景描述文本、`status`【活跃 / 归档】）。
- `mission`：长线事项基础信息。
- `task`：各子会话阶段性、每日待办任务。
- `raw_chat_log`：原始对话归档表（只做素材，不参与推理检索）。

### 3.2 定时增量提炼流程

由行动层定时任务触发，每 2 小时执行一次：

1. 按用户维度，查询近 2 小时 `raw_chat_log` 原始对话（包含主对话 + 所有子对话新增聊天）。
2. 调用 LLM 增量提炼：过滤无效闲聊，只抽取长期有效的事实信息，生成 `user_memory` 记录。
3. 生成记忆后，自动发起关联推理：判断本条记忆适配哪些活跃 `sub_session`。
4. 推理结果写入 `memory_session_rel` 关联表，自动去重，避免重复绑定。

### 3.3 记忆检索权限控制（核心隔离逻辑）

- **主对话检索记忆**：直接查询 `user_memory`，读取该用户全部记忆摘要（无过滤条件）。
- **子对话检索记忆**：
  1. 先通过自身 `sub_session_id` 查询 `memory_session_rel`，拿到所有绑定的 `memory_id`；
  2. 再根据 `memory_id` 去 `user_memory` 读取对应记忆；
  3. 任何未建立绑定关系的记忆，子对话无法读取。

### 3.4 懒加载机制

记忆摘要轻量化存放；如需查看原始对话上下文，通过记忆 ID 反向检索 `raw_chat_log`，不会一次性加载海量原始文本，控制 LLM 上下文 token 消耗。

## 四、行动层

行动层负责自动化任务与调度引擎，无 LLM 依赖。

### 4.1 任务调度引擎

持续读取 `task` 任务表，按时向用户推送待办提醒；当子会话任务调整完成，自动清理过期任务、写入新任务。模型调用异常时，推送提醒功能独立可用。

### 4.2 全局唯一定时 Job：增量记忆提炼任务（周期 2 小时）

执行流程：

1. 拉取窗口内所有新增原始对话日志；
2. 送入感知层 LLM 执行增量提炼，筛选有效记忆存入 `user_memory`；
3. 调用关联推理，生成记忆与子会话的绑定关系写入关联表；
4. 无「统一向主会话同步摘要」步骤，主会话天生具备读取全部记忆的权限。

### 4.3 兜底容错机制

1. 关联推理失败降级：仅生成记忆，不创建绑定，等待下一轮定时任务重试。
2. 关联关系写入自动去重，防止同一记忆 ID + 子会话 ID 重复插入。
3. 记忆分页加载，单次查询限制条数，避免上下文溢出。
4. 子会话归档仅修改 `status` 状态，记忆、关联表数据永久保留，支持历史复盘。

## 五、架构核心优势

1. 逻辑大幅简化，仅保留一套 2 小时定时增量提炼任务，移除实时事件同步链路。
2. 严格区分「原始对话日志」和「提炼后的长期记忆」，避免无效闲聊占用上下文。
3. 中立记忆库设计：记忆本身不属于任何会话，权限依靠关联表动态控制。
4. 主对话默认全量可见；子会话依靠 LLM 智能判断按需授权访问。
5. 两张数据表实现多对多关系，数据库兼容性好，查询、统计、分页友好。
6. 标准 Agent 三层架构职责清晰：感知负责输入与模型推理，记忆负责存储 + 权限隔离，行动层承载所有自动化定时任务。
7. 架构天然可扩展：后续新增任意子会话，仅分配 `sub_session_id` 与 `session_desc`，记忆提炼、关联逻辑完全无需改动。

## 六、工程实现要点

- 基于 Java 21 + Spring Boot + Spring AI Alibaba。
- 采用 DDD 架构，每个场景（子会话类型）作为独立的域。
- 支持场景域热部署，新增目标类型无需整体重启。
