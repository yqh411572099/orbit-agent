# 版本更新记录

只记录大项功能变化；bug 修复与细节调整不在此列。

## v1.0.1

- **上下文锚定**：回答与记录前，先把"今天/昨天/春节/附近"等相对时间和指代，消解成确定的日期、主体与对象；锚定阶段也能调用工具查实，避免跨日串数据、时间口径错乱。
- **确定性计算能力**：数字四则运算、日期推算（相差几天/几周、星期几、推算日期）从模型心算中剥离，改由专用计算能力给出可复现结果，模型不再凭感觉算数字。
- **外部能力开关**：每个主对话/子对话可独立开启或关闭联网搜索、地图检索这两类按量计费的外部能力；关闭后不再调用，本地知识库与计算等能力不受影响。该开关只在界面设置，对话内容无权更改。

## v1.0.0

首个正式版本，主体能力：

- **多目标长期管家**：一个主对话入口，可为每件长期目标（孕期、考研、考证、减重/通用计划等）开启独立子对话，互不干扰、独立跟进。
- **长期记忆**：原始对话归档、周期性提炼成结构化长期记忆，支持冲突/过期治理，主对话全量可见、子对话按相关性共享。
- **时间轴与待办提醒**：目标拆成随时间推进、可重排的待办，按关注项分组、到点主动提醒，替代一次性计划输出。
- **场景热插拔**：场景以独立"域"接入，新增目标类型不改主链路；内置孕期、考研、考证、通用计划。
- **工具体系**：联网搜索、地图/周边检索、可沉淀复用的知识库、语音输入播报、浏览器定位。
- **数据可视化**：模型为目标声明可追踪指标，日常汇报即记录，卡片与图表展示趋势。
- **基础能力**：账号体系、流式对话、思考过程展示、变更确认弹窗、对话历史增量同步。
